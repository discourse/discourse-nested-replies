import Controller from "@ember/controller";

export default class TopicNestedController extends Controller {
  // This controller uses the parent topic controller's model
  // We don't extend TopicController to avoid query param conflicts
}
